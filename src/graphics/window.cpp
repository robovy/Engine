#include "window.h"

namespace engine
{
        namespace graphics
        {
                Window::Window(const char *name int width int height)
                {
                        m_Name = name;
                        m_Width = width;
                        m_Height = height;
                }
        }
}
